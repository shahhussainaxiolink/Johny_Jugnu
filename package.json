{
  "name": "johnny-jugnu",
  "version": "1.0.0",
  "private": true,
  "description": "Railway entrypoint for the Johnny & Jugnu backend service",
  "scripts": {
    "start": "npm start --prefix backend",
    "railway:build": "npm ci --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
